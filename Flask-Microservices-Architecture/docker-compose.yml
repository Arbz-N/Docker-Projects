version: "3.8"

services:
  auth-service:
    build: auth-service
    ports:
      - "8001:8000"
    environment:
      - MONGO_URI=${MONGO_URI}
  order-service:
    build: order-service
    ports:
      - "8002:8000"
    environment:
      - MONGO_URI=${MONGO_URI}
  payment-service:
    build: payment-service
    ports:
      - "8003:8000"
    environment:
      - MONGO_URI=${MONGO_URI}
